# Database Configuration (read by Prisma CLI - required)
DATABASE_URL="postgresql://username:password@localhost:5433/greedadvisor"

# For local development with Docker Compose (port 5433)
# DATABASE_URL_DOCKER="postgresql://user:password@localhost:5433/greedadvisor"

# For Neon (serverless Postgres):
# DATABASE_URL="postgresql://<user>:<password>@<host>/<db>?sslmode=require"
